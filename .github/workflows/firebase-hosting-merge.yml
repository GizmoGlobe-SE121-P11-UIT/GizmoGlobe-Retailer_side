# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Get Flutter dependencies
        run: flutter pub get
      - name: Create dummy .env file
        run: touch .env
      - name: Build Flutter web with compile-time config
        run: flutter build web --release --dart-define-from-file=.env.web
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SE121P11_GIZMOGLOBE }}
          channelId: live
          projectId: se121p11-gizmoglobe
          target: gizmoglobe-retailer
